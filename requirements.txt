pandas
plotly
streamlit
requests
scikit-learn
statsmodels
pytest
streamlit-option-menu
streamlit-extras
streamlit-aggrid

# Filmweek (AI discovery) LLM backends. anthropic is the default; the others are
# optional and only imported when you select that provider in the UI.
#   OpenAI / OpenRouter / Ollama all use the `openai` package.
#   Google Gemini uses `google-genai`.
anthropic
openai
google-genai

# Optional: enables *semantic* theme similarity (sentence-transformer embeddings of
# keywords + overview). Without it, theme similarity falls back to TF-IDF over the same
# text. Pulls in torch, so it is intentionally not a hard dependency.
# pip install sentence-transformers
